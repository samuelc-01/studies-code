/*let numeros = (number1, number2) => {

}

if{number1 === 0
return "Escreva um número maior que zero"}


console.log(50, 25);
const percentage = (nominator, denominator) => {
    var result = nominator / denominator;
    console.log(nominator + "quivale a " + result * 100 + "% de " + denominator);
};

percentage(20, 100);
*/

let pessoa = (nome, sobrenome, idade)

const dell = 